import React from "react";
import { useParams } from "react-router-dom";

const Checkout = () => {
  // const { id } = useParams();
  return (
    <div>
      <h3>Checkout </h3>
    </div>
  );
};

export default Checkout;
